import java.util.*;

class Solution {
    public ArrayList solution(int start, int end) {
        ArrayList answer = new ArrayList();
        for (int num=start; num<=end; num++){
            answer.add(num);
        }
        return answer;
    }
}